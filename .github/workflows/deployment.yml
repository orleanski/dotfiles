name: Deployment CI

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run getdotfiles.sh
    - shell: bash
      run: |
        ./getdotfiles.sh
